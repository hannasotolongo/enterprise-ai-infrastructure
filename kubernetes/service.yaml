apiVersion: v1
kind: Service

metadata:
  name: enterprise-ai-service

spec:
  selector:
    app: enterprise-ai-inference

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: NodePort